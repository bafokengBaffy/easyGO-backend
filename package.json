{
  "name": "easygo-backend-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web-backend && npm install && npm run build",
    "start": "cd web-backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@types/jest": "^30.0.0",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
